{
  "name": "Build and Test",
  "on": [
    {
      "push": {
        "branches": [
          "main"
        ]
      }
    }
  ],
  "jobs": [
    {
      "name": "Build and Test",
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "uses": "actions/checkout@v3",
          "name": "Checkout code"
        },
        {
          "name": "Mock build",
          "run": "echo 'Build successful' && exit 0"
        }
      ]
    }
  ]
}